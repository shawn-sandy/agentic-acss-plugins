---
name: release-check
description: Audit release paperwork for a plugin before opening a PR. Merged into release-plugin --check.
argument-hint: <plugin-name>
disable-model-invocation: false
---

# /release-check

> **Merged:** This skill is superseded by `release-plugin <plugin-name> --check`.
> Use `/release-plugin <plugin-name> --check` for the release paperwork audit.
> The canonical skill lives at `.claude/skills/release-plugin/SKILL.md`.

Usage: `/release-plugin <plugin-name> --check`
