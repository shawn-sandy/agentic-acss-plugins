---
name: validate-plugin
description: Deep structural validation of a single plugin before publishing. Merged into validate-plugins --scope=plugin.
disable-model-invocation: false
---

# /validate-plugin

> **Merged:** This skill is superseded by `validate-plugins --scope=plugin`.
> Use `/validate-plugins <plugin-name> --scope=plugin` for deep single-plugin validation.
> The canonical skill lives at `.claude/skills/validate-plugins/SKILL.md`.

Usage: `/validate-plugins <plugin-name> --scope=plugin`
